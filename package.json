{
  "name": "church-directory-site",
  "private": true,
  "description": "Static GitHub Pages site — use npm run preview for local live reload",
  "scripts": {
    "preview": "live-server --host=0.0.0.0 --port=5500 --open=http://localhost:5500/index.html",
    "preview:python": "python -m http.server 5501 --bind 0.0.0.0"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
